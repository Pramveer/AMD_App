import { Template } from 'meteor/templating';

import './CompareToggleButton.html';

Template.CompareToggleButton.rendered = function() {
// To do when rendered
};


Template.CompareToggleButton.events({
    // To do to add event 
});